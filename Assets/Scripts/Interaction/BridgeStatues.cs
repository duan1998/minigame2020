using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BridgeStatues : MonoBehaviour
{
    public bool isLock = false;
    // Start is called before the first frame update
    void Start()
    {
        
    }
    private void Awake() {
        isLock = false;
    }
    // Update is called once per frame
    void Update()
    {
        
    }
}
